# The Collective Strategist - Server Environment Variables
# TEMPLATE FILE - Copy to .env.server and update with real values
# 
# Instructions:
# 1. cp .env.server.example .env.server
# 2. Run ./setup-database.sh to get POSTGRES_PASSWORD
# 3. Generate JWT_SECRET with: openssl rand -base64 32
# 4. Update ACME_EMAIL to your admin email address
# 5. Edit other values as needed

# Database Configuration (separate user for security isolation)
POSTGRES_PASSWORD=CHANGE_ME_AFTER_RUNNING_DATABASE_SETUP
JWT_SECRET=your-secure-jwt-secret-here

# Application Configuration
NODE_ENV=production
CORS_ORIGIN=https://strategist.greenfieldoverride.com,http://localhost:8090
LOG_LEVEL=info
RATE_LIMIT_ENABLED=true

# Ports (matching Caddy configuration)
API_PORT=8007
FRONTEND_PORT=8090

# Domain Configuration
DOMAIN=greenfieldoverride.com
APP_DOMAIN=strategist.greenfieldoverride.com

# SSL/TLS Configuration (change this to your actual admin email)
ACME_EMAIL=CHANGE_ME_TO_YOUR_ADMIN_EMAIL